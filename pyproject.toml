[project]
name = "mechanistic-interpretability-truth-direction"
version = "0.1.0"
description = "A mechanistic interpretability project on fact-pair label signals, completion compatibility, and intervention limits."
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.black]
line-length = 100
target-version = ["py310"]
